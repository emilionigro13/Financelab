FinanceLab — Development Log

A day-by-day journal of building FinanceLab, a professional financial analysis platform. This document tracks what was built, what was learned, and what went wrong.

Day 1 — Project Planning & Architecture

Goal: Define the project architecture, folder structure, and initialize version control — no feature code written.
What was done: Decided on a decoupled monorepo architecture with separate frontend/ (Next.js) and backend/ (Express) services communicating via REST API instead of a unified full-stack Next.js app. Created the base folder structure: frontend/, backend/, docs/, along with README.md, ROADMAP.md, .gitignore, and docs/ARCHITECTURE.md. Initialized a local Git repository and made the first commit. Created a public GitHub repository and connected it to the local repo.
Issues encountered & resolved: Git was not recognized in the terminal (CommandNotFoundException) — caused by installing Git while VS Code was already open, so the PATH hadn't refreshed. Fixed by fully closing and reopening VS Code after installation. Git identity on the local repo was initially a placeholder — updated with real name/email to match the GitHub account before pushing.
Concepts learned: Difference between a monolithic and a decoupled (frontend/backend) architecture. Why architecture decisions should be documented (ARCHITECTURE.md) as part of the engineering process, not as an afterthought. Git fundamentals: init, remote add origin, branch -M main, push -u origin main, and why the -u flag matters. How PATH and terminal sessions interact with newly installed CLI tools on Windows.
Commit: chore: initialize project structure and architecture planning

Day 2 — Backend Skeleton with Express

Goal: Build a minimal Express server with a single /health endpoint, to validate the Node → Express → HTTP response chain before adding any real business logic.
What was done: Initialized a Node.js project inside backend/ with npm init -y. Installed Express (^5.2.1) as a dependency and nodemon as a dev dependency for automatic server restarts. Created server.js with a single GET /health route returning { status: "ok", timestamp: }. Configured package.json scripts: dev (nodemon) and start (plain node). Verified the server locally at http://localhost:4000/health.
Issues encountered & resolved: npm was not recognized in PowerShell (CommandNotFoundException) — Node.js was not yet installed. Fixed by installing Node.js LTS via the official Windows installer and restarting VS Code. After installing Node.js, npm still failed with a PSSecurityException — PowerShell's execution policy was blocking script execution. Fixed by running Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser. npm run dev initially failed with Missing script: "dev" — the package.json edit had not been saved to disk before running the command. Fixed by saving before running. Windows Firewall prompted to allow Node.js network access on first server start — expected behavior; allowed for local development. Visiting http://localhost:4000/ returned Cannot GET / — the server only defines a /health route, so the root path is correctly unhandled. Fixed by navigating to the correct endpoint.
Concepts learned: Role of package.json as a project's manifest. Difference between dependencies and devDependencies. How an Express route is defined and how req/res work. Why a /health endpoint is a standard practice in real backends. Practical debugging: distinguishing "code looks correct" from "code is actually saved and running." Windows-specific development friction: PATH refresh, PowerShell execution policy, firewall prompts on first network use.
Commit: feat: add Express backend with health-check endpoint

Day 3 — TypeScript Migration & Database Setup with Prisma/Supabase

Goal: Migrate the backend from plain JavaScript to TypeScript, and connect it to a real PostgreSQL database (hosted on Supabase) via Prisma ORM, creating the first two data models.
What was done: Migrated the backend from JavaScript (server.js, CommonJS) to TypeScript (src/index.ts, ES Modules). Isolated application source code under a src/ directory. Configured tsconfig.json with rootDir: "./src", outDir: "./dist", and strict mode. Installed and configured Prisma ORM. Created a Supabase project (PostgreSQL, Frankfurt region). Defined the initial data models in schema.prisma: User and Portfolio, with a one-to-many relation and onDelete: Cascade. Configured database connection through environment variables. Ran the first real migration (npx prisma migrate dev --name init).
Issues encountered & resolved: Nodemon crashed when trying to run TypeScript files directly — resolved by installing ts-node and @types/express. A quote-mismatch typo in an import statement broke the TypeScript compiler — fixed by correcting the string delimiters. A missing comma in tsconfig.json caused broken JSON configuration, silently disabling editor type-checking — fixed by rewriting the file with strict JSON syntax. Prisma 7 removed support for url and directUrl directly inside schema.prisma (error P1012) — Prisma 7 moved connection configuration into a separate prisma.config.ts. Fixed by rewriting schema.prisma to contain only provider = "postgresql" and moving connection strings into prisma.config.ts via environment variables (DIRECT_URL for CLI migrations, DATABASE_URL for runtime via Supabase's pooler).
Concepts learned: TypeScript vs JavaScript: type safety, compile-time errors, better IDE support. ES Modules vs CommonJS. tsconfig.json options. Prisma ORM architecture: schema definition, client generation, migrations. Database schema design: models, relations, foreign keys, cascade deletes. PostgreSQL data types: UUID, String, DateTime, Decimal. One-to-many relationships. Environment variable security for database connections. Prisma migration workflow.
Commit: feat: migrate backend to TypeScript and setup Prisma/Supabase

Day 4 — Environment Configuration & Validation

Goal: Set up environment configuration validation with Zod, ensuring all required environment variables are present and correctly formatted before the server starts.
What was done: Installed Zod for schema validation. Created centralized config.ts module that validates all environment variables at startup. Implemented type coercion (string → number, string → enum). Added fail-fast validation: server exits immediately with clear error messages if config is invalid. Created .env.example with documentation of all required variables. Structured config object with nested categories: server, database, security, api.
Issues encountered & resolved: Environment variables not loaded — DATABASE_URL: Required even though .env existed. Root cause: Node.js doesn't automatically read .env files; process.env only contains system variables. Fixed by installing dotenv and adding import 'dotenv/config' at the top of config.ts. Type coercion confusion — PORT was string instead of number. Root cause: Environment variables are always strings. Fixed by using z.coerce.number().
Concepts learned: Fail Fast Principle: If something will fail, fail immediately with a clear error. Configuration as Code: Centralized config instead of scattered process.env references. Type Coercion: Converting environment variable strings to proper types. Schema Validation: Declarative validation with Zod. Environment Variable Security: Never commit .env, always use .env.example.
Commit: feat: add environment configuration validation with Zod

Day 5 — Frontend Skeleton with Next.js

Goal: Initialize the Next.js frontend with TypeScript, TailwindCSS, and a professional folder structure.
What was done: Initialized Next.js 15 project with TypeScript and App Router. Configured TailwindCSS with custom theme (FinanceLab brand colors: navy, emerald). Created professional folder structure: app/, components/ui/, lib/. Built landing page with hero section, feature cards, stats, and CTA. Added global styles with CSS variables for theming (light/dark mode ready). Configured fonts: Inter for UI text, JetBrains Mono for financial data. Added SEO metadata with OpenGraph and Twitter cards. Created reusable UI components: Button, Card with class-variance-authority.
Issues encountered & resolved: Missing tailwindcss-animate — Build error Cannot find module 'tailwindcss-animate'. Root cause: Plugin referenced in tailwind.config.ts but not in package.json. Fixed by npm install tailwindcss-animate. Component import errors — Cannot find module '@/components/ui/button'. Root cause: Path alias @/ not configured or files in wrong location. Fixed by verifying tsconfig.json paths and component file locations.
Concepts learned: Next.js App Router vs Pages Router. Server Components vs Client Components ('use client'). TailwindCSS utility-first approach. CSS variables for theming. class-variance-authority for type-safe component variants. cn() utility with clsx + tailwind-merge. SEO best practices with Next.js Metadata API.
Commit: feat: initialize Next.js frontend with TypeScript and Tailwind

Day 6 — API Client Setup, CORS Configuration & Dashboard Layout

Goal: Set up API client configuration with CORS, create a connection between frontend and backend, and build a dashboard layout.
What was done: Backend: Configured CORS middleware with origin whitelist (FRONTEND_URL). Added API versioning prefix (/api/v1/). Enhanced health endpoint with database connectivity check. Added structured error handling with consistent JSON response format. Added request logging middleware. Frontend: Created typed API client (lib/api.ts) with apiGet and apiPost. Added environment variable NEXT_PUBLIC_API_URL. Built Dashboard Layout with sidebar navigation and header. Created Dashboard Overview page with connection status cards. Added responsive design for sidebar.
Issues encountered & resolved: dotenv not loading .env file — DATABASE_URL: Required even with .env present. Fixed by installing dotenv and adding import 'dotenv/config'. Prisma Client not initialized — @prisma/client did not initialize yet. Fixed by running npx prisma generate. Prisma migrate stuck on Windows PowerShell — npx prisma migrate dev froze, not accepting y/n input. Fixed by using npx prisma db push instead (non-interactive). Connection pooler blocking migrations — db push worked but migrate dev failed silently. Root cause: Supabase connection pooler (port 6543) doesn't support schema changes. Fixed by using direct connection (port 5432) for migrations, then switched back to pooler for app. Missing tailwindcss-animate dependency — Build error. Fixed by npm install tailwindcss-animate.
Concepts learned: CORS mechanics: preflight requests, allowed origins, credentials. API client patterns: centralized client vs scattered fetch calls. Dashboard layout architecture: sidebar + header + main content. Responsive navigation patterns. Environment variables in Next.js: NEXT_PUBLIC_ prefix for browser access. Type-safe API responses with TypeScript generics. Connection pooling vs direct database connections.
Commit: feat: setup API client, CORS, and dashboard layout

Day 7 — User Authentication (Register/Login) with bcrypt & JWT

Goal: Build a complete authentication system: registration, login, password hashing with bcryptjs, and JWT token generation.
What was done: Created src/utils/password.ts with hashPassword() and verifyPassword() using bcryptjs (12 salt rounds). Created src/utils/jwt.ts with generateToken() and verifyToken(). Created src/middleware/auth.middleware.ts with authenticateToken() to protect routes (Bearer pattern). Created src/routes/auth.routes.ts with POST /api/v1/auth/register and POST /api/v1/auth/login. Updated src/index.ts to mount auth routes. Updated prisma/schema.prisma adding firstName and lastName to the User model. Configured .env with JWT_SECRET and JWT_EXPIRES_IN. Connected the backend to the PostgreSQL database on Supabase.
Issues encountered & resolved: Cannot find module 'cors' — cors and @types/cors packages were missing. Fixed with npm install cors and npm install -D @types/cors. Cannot find module '../routes/health.routes.js' — conflict between TypeScript NodeNext (requires .js in imports) and tsx. Fixed by changing tsconfig.json from "module": "NodeNext" to "module": "ESNext" and "moduleResolution": "bundler", removing .js extensions from relative imports. No overload matches this call on jwt.sign() — expiresIn type was not compatible. Fixed by reading JWT_SECRET and JWT_EXPIRES_IN directly from process.env in jwt.ts. @prisma/client did not initialize yet — Fixed with npx prisma generate. DIRECT_URL: Required — Added to satisfy Zod validation. Can't reach database server at localhost:5432 — local PostgreSQL was not running. Decided to migrate to Supabase by inserting real connection strings into .env. Route POST /api/v1/auth/register not found — another Node process was using port 4000. Fixed by killing all node processes with taskkill /F /IM node.exe and restarting. Unknown argument 'firstName' — Prisma schema only had a name field. Fixed by updating schema.prisma and re-running npx prisma db push. Prisma db push stuck on Supabase port 6543 — the connection pooler does not support DDL. Fixed by temporarily using port 5432 (direct connection), then restoring port 6543 for app runtime. EPERM: operation not permitted during prisma generate — file locked by a previous process. Fixed with taskkill /F /IM node.exe and deleting node_modules\.prisma folder before regenerating.
Concepts learned: bcryptjs vs bcrypt: bcryptjs is pure JavaScript, no native dependencies. JWT architecture: header (algorithm), payload (claims), signature (HMAC). The token is stateless. Bearer token pattern: Authorization: Bearer <token> is the de-facto standard for protected REST APIs. Timing attack prevention: use the same error message ("Invalid credentials") for both non-existent email and wrong password. Zod schema validation for user input: email format, password strength. Prisma connection pooler (port 6543) vs direct connection (port 5432). moduleResolution: "bundler" in TypeScript: allows relative imports without .js extension, compatible with tsx.
Commit: feat: add user authentication with bcrypt and JWT

Day 8 — Frontend Authentication Pages (Login/Register) with React Context, Zod & Protected Routes

Goal: Build the frontend authentication layer: login and register pages, global auth state via React Context, JWT persistence in localStorage, client-side form validation, and protected dashboard routes.
What was done: Created src/lib/auth-context.tsx with React Context API to manage global auth state (user, token, isLoading). Created src/app/providers.tsx as a Client Component bridge to wrap the app with AuthProvider inside the RootLayout. Updated src/app/layout.tsx to use Providers. Created src/app/login/page.tsx with controlled form, Zod client-side validation, error handling, and redirect to /dashboard on success. Created src/app/register/page.tsx with controlled form, Zod validation including cross-field password matching (.refine), and redirect to /dashboard. Created src/components/auth/AuthGuard.tsx to protect the dashboard: redirects unauthenticated users to /login and shows a loading spinner. Created src/components/user-nav.tsx to display the logged-in user's name/email and a logout button. Created src/app/dashboard/layout.tsx wrapping children with AuthGuard. Created src/app/dashboard/page.tsx as the main dashboard view with placeholder stats cards. Updated src/app/page.tsx to render UserNav in the header. Updated src/lib/api.ts to align token storage key with localStorage ("token") and inject the Bearer Authorization header. Added frontend environment variable NEXT_PUBLIC_API_URL in .env.local.
Issues encountered & resolved: Module '"@/lib/api"' has no exported member 'apiPost' — the existing api.ts only exported apiPost as a standalone function but the import path was failing. Fixed by verifying the exact export structure. Cannot find module './providers' — Providers component was created but TypeScript had not picked it up. Fixed by saving the file and restarting the TypeScript language server. Cannot find module or type declarations for './globals.css' — next-env.d.ts was missing or corrupted. Fixed by recreating frontend/next-env.d.ts with the /// <reference types="next" /> directives. Property 'errors' does not exist on type 'ZodError' — in Zod v3, the correct property is .issues, not .errors. Fixed by replacing result.error.errors with result.error.issues. Type 'symbol' cannot be used as an index type on issue.path[0] — Zod issue.path is typed as (string | number)[]. Fixed by adding a type guard: const field = issue.path[0]; if (typeof field === 'string') { ... }. Parameter 'err' implicitly has an 'any' type in catch blocks — TypeScript strict mode requires typed catch variables. Fixed by replacing catch (err: any) with catch (err) and using err instanceof Error ? err.message : '...'. Missing dependency tailwindcss-animate — build failed. Fixed with npm install tailwindcss-animate. Missing dependency zod — Zod was used for form validation but not listed in frontend package.json. Fixed with npm install zod. Dashboard returned 404 after login — the /dashboard route existed as a layout but had no page.tsx. Fixed by creating src/app/dashboard/page.tsx. "Email already registered" on second registration attempt — the Supabase PostgreSQL database is persistent. Fixed by using a new unique email for each test or manually deleting test rows. Supabase sent an email confirmation code — Supabase Auth has email confirmations enabled by default, but our custom Express auth system does not use Supabase Auth. The confirmation email can be safely ignored.
Concepts learned: React Context API for global state: createContext, useContext, and the Provider pattern eliminate prop drilling. Controlled forms in React: binding input values to useState gives full control over validation, formatting, and submission logic. Zod client-side validation: safeParse avoids throwing exceptions; .issues provides structured error data; .refine enables cross-field validation. Type narrowing in TypeScript: typeof checks and instanceof Error prevent implicit-any errors. localStorage as a JWT persistence layer: tokens survive page refreshes but are vulnerable to XSS. This is acceptable for an MVP but will be replaced with httpOnly cookies in production. Protected routes in Next.js App Router: a Client Component (AuthGuard) with useEffect and useRouter can intercept navigation and redirect unauthenticated users. Next.js environment variables: only variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
Commit: feat: add frontend authentication pages with JWT and protected routes

Day 9 — User Profile & Account Settings

Goal: Build a complete profile settings page where authenticated users can update their information, change their password (requiring the old one), and delete their account with a confirmation modal.
What was done: Backend: Created src/middleware/auth.middleware.ts with authenticateToken() to extract and verify JWT from the Authorization: Bearer <token> header, attaching user payload to req.user. Extended src/routes/auth.routes.ts with four new protected endpoints: GET /api/v1/auth/me — returns current user data from the database; PUT /api/v1/auth/me — updates profile fields with partial validation; PUT /api/v1/auth/password — changes password only after verifying the old password with bcrypt; DELETE /api/v1/auth/me — permanently deletes the user account; Prisma cascade removes associated Portfolio records. Added Zod schemas for each new endpoint with .optional() for partial updates and .refine() to reject empty PUT /me requests. Frontend: Added apiPut() and apiDelete() to src/lib/api.ts with automatic Bearer token injection. Extended AuthContext with updateUser(data: Partial<User>) to merge partial updates into React state and localStorage instantly. Created src/app/dashboard/profile/page.tsx with three sections: Profile Information form (firstName, lastName, email) with success/error feedback; Change Password form requiring oldPassword, newPassword, and confirmPassword; Danger Zone with Delete Account button and a custom confirmation modal (fixed overlay, no external library). Updated src/app/dashboard/page.tsx to include a "Profile" link in the header.
Issues encountered & resolved: Type '"/dashboard/profile"' is not assignable to type 'RouteImpl<...>' — Next.js 15 has strict route typing for the Link component href prop. Fixed by casting the href with as any as a temporary measure. Argument of type 'User | null' is not assignable to parameter of type 'Partial<User>' — after calling apiPut('/auth/me'), the response type declared user as potentially null. Fixed by explicitly typing the apiPut generic with the exact user shape and adding a runtime guard if (res.data.user) before calling updateUser(). Missing next-env.d.ts caused Cannot find module './globals.css' again — the file was overwritten or deleted during file operations. Fixed by recreating frontend/next-env.d.ts and restarting the TypeScript server. Dashboard returned 404 after login — /dashboard/layout.tsx existed with AuthGuard but there was no page.tsx inside the dashboard folder. Fixed by creating src/app/dashboard/page.tsx.
Concepts learned: Express middleware pattern: middleware runs before route handlers, enabling authentication gating at the route level. TypeScript Partial<T> utility: converts all properties of a type to optional, perfect for PATCH/PUT partial update payloads. Bearer token extraction: splitting Authorization: Bearer <token> and verifying with jwt.verify. Timing attack prevention on password change: verifying oldPassword with bcrypt before rejecting ensures attackers cannot enumerate valid tokens by observing response times. Custom modal without libraries: a fixed-position div with z-50 and a semi-transparent backdrop is lighter than installing Radix Dialog for a simple confirmation. Database cascade deletes: Prisma's onDelete: Cascade on the User-Portfolio relation automatically cleans up dependent records when an account is deleted.
Commit: feat: add user profile settings with update, password change and account deletion

Day 10 — Company Search & Stock Detail with Live Market Data

Goal: Integrate real financial data via Finnhub API: stock search with autocomplete, live quotes, company profiles, and 60-day price charts.
What was done: Backend: Created src/utils/cache.ts with an in-memory Cache class featuring TTL (time-to-live) expiration to respect Finnhub's 60 calls/minute rate limit. Created src/services/finnhub.service.ts as the Finnhub client with four methods: searchSymbols, getQuote, getCompanyProfile, getCandles. Implemented graceful degradation: when Finnhub returns 403 on /stock/candle (not included in the free plan), the service catches the error and falls back to generateMockCandles(), producing 60 days of realistic synthetic data based on the current quote price. Created src/routes/market.routes.ts exposing four endpoints under /api/v1/market: GET /search?q= — symbol search with autocomplete; GET /quote/:symbol — real-time price, change, open, high, low, previous close; GET /company/:symbol — company profile; GET /candles/:symbol — historical daily candles (60 days), with mock fallback. Frontend: Installed recharts for React-native chart rendering. Created src/components/search/SearchBar.tsx with debounce pattern (300ms delay), dropdown results with symbol/company name/type, keyboard navigation support, and click-outside-to-close. Created src/components/charts/StockChart.tsx rendering a responsive LineChart with CartesianGrid, XAxis, YAxis, custom Tooltip, emerald green line matching FinanceLab brand, loading spinner and error states. Created src/app/dashboard/stock/[symbol]/page.tsx as a dynamic route: fetches quote and profile in parallel with Promise.all, displays company logo/name/ticker/industry/country, shows live price with color-coded daily change, four stat cards (Open, High, Low, Previous Close), embeds the 60-day StockChart. Added "Search" link to the dashboard header. Updated dashboard grid with a "Search Stocks" card.
Issues encountered & resolved: Finnhub API returned 403 Forbidden on /stock/candle — root cause: the free plan does not include historical candle data. Fixed by wrapping getCandles in a try/catch block that falls back to generateMockCandles() when the real API fails. The mock generator creates realistic random-walk prices seeded around the current quote. Unterminated string constant in stock/[symbol]/page.tsx — root cause: the file was truncated during copy-paste. Fixed by rewriting the complete file with all closing tags and brackets. Missing User-Agent header causing intermittent Cloudflare blocks — fixed by adding 'User-Agent': 'FinanceLab/1.0' to all Finnhub fetch calls.
Concepts learned: API rate limiting and caching strategies: TTL-based in-memory cache prevents redundant external API calls and respects provider limits. Debounce pattern: delaying execution until the user stops typing reduces network load and improves perceived performance. Graceful degradation: when a third-party API feature is unavailable (paid tier), the system falls back to synthetic data rather than crashing. Dynamic routes in Next.js App Router: [symbol] captures any URL segment and passes it as a parameter. Parallel data fetching: Promise.all([quote, profile]) loads independent resources simultaneously instead of sequentially. Recharts library: composable React chart components with full theming control.
Commit: feat: integrate Finnhub API for live stock search, quotes and price charts

Day 11 — Personal Watchlist

Goal: Allow authenticated users to save stocks to a personal watchlist, view it on the dashboard with live prices, and remove items.
What was done: Database: Added Watchlist model to prisma/schema.prisma with fields: id, userId, symbol, companyName, createdAt. Enforced composite unique constraint @@unique([userId, symbol]). Added onDelete: Cascade on the User relation. Created the table manually via Supabase SQL Editor (db push blocked by network/port 5432 restriction). Backend: Created src/routes/watchlist.routes.ts with three protected endpoints: GET /api/v1/watchlist — lists all items; POST /api/v1/watchlist — adds a stock, returns 409 if already present; DELETE /api/v1/watchlist/:id — removes an item using deleteMany({ id, userId }) to guarantee users can only delete their own entries. Mounted watchlist routes in src/index.ts. Frontend: Updated src/app/dashboard/page.tsx with a "Your Watchlist" section that fetches the watchlist on mount, fetches live quotes for each symbol in parallel via Promise.all, displays cards with symbol, company name, price, and daily change, links each card to the stock detail page. Updated src/app/dashboard/stock/[symbol]/page.tsx with a toggle button "Add to Watchlist" / "Remove from Watchlist", checks if the current stock is already in the watchlist, calls POST /watchlist or DELETE /watchlist/:id on click.
Issues encountered & resolved: Property 'watchlist' does not exist on type 'PrismaClient' — Prisma Client was not regenerated after adding the Watchlist model. Fixed by running npx prisma generate after creating the table in Supabase. EPERM: operation not permitted during prisma generate — the backend server was still running, locking the Prisma query engine DLL. Fixed by killing all node processes with taskkill /F /IM node.exe, deleting node_modules\.prisma\client, and regenerating. P1001: Can't reach database server at port 5432 — the direct PostgreSQL port is blocked by the local network. Fixed by creating the Watchlist table manually via Supabase SQL Editor. Supabase prompted about Row Level Security (RLS) — Supabase warns when creating tables without RLS. Fixed by clicking "Run without RLS" since our security is enforced at the Express middleware level. Type 'string | string[] | undefined' is not assignable to type 'string' on req.params.id — Express types req.params as potentially an array. Fixed by explicitly casting const id = req.params.id as string.
Concepts learned: Composite unique constraints: @@unique([userId, symbol]) enforces business rules at the database level, eliminating race conditions. deleteMany vs delete for security: deleteMany({ id, userId }) is idempotent and silently fails if the record doesn't belong to the user, preventing information leakage. N+1 query problem: fetching quotes for each watchlist item individually would be inefficient at scale; mitigated with Promise.all in the frontend. Optimistic UI pattern: the Add/Remove button state updates immediately after API success, giving instant feedback without waiting for a full page reload.
Commit: feat: add personal watchlist with add/remove and live prices

Day 12 — Financial Ratios Calculator

Goal: Build an Analysis Engine that transforms raw fundamental data from Finnhub into 12 structured financial ratios, each enriched with formulas, descriptions, and health signals, and add a tab-based UI to the stock detail page separating market data from fundamental analysis.
What was done: Backend: Extended finnhub.service.ts with getBasicFinancials() calling /stock/metric?metric=all endpoint with a 3600-second TTL cache. Created analysis.engine.ts as the core business logic layer mapping raw Finnhub metric keys like peBasicExclExtraTTM, roeRfy, and currentRatioQuarterly into a typed AnalysisResult containing 12 FinancialRatio objects grouped into five categories: Valuation, Profitability, Leverage, Liquidity, and Dividends. Each ratio carries a raw value, a formatted human-readable string, a mathematical formula, an investor-oriented description, a category, and a signal evaluated through dual functions getSignal() for higher-is-better metrics and getInverseSignal() for lower-is-better metrics using standard industry thresholds. Added GET /api/v1/market/analysis/:symbol endpoint. Frontend: Created RatioCard.tsx as a self-contained card showing the ratio name, category badge, formatted value, colored signal badge, and progressive disclosure on click to reveal formula and description. Created FinancialRatios.tsx as a container fetching the analysis endpoint and rendering ratios grouped by category in a responsive grid. Refactored the stock detail page to introduce Overview and Financial Analysis tabs.
Issues encountered & resolved: Some Finnhub metric keys return undefined for non-US companies or recent IPOs with insufficient TTM data such as pegRatio or dividendYieldIndicatedAnnual. Fixed by defaulting to null and rendering N/A with an unknown signal badge, requiring careful null-coalescing to avoid NaN propagation into formattedValue.
Concepts learned: Financially: difference between absolute valuation and growth-adjusted valuation via PEG; why ROE and ROA must be read together to detect leverage effects; that Quick Ratio is a stricter liquidity test than Current Ratio because it excludes inventory; that sector context is critical since a P/E of 25 is expensive for a utility but normal for SaaS. From a computer science perspective: Data Transformation Layer pattern separating the raw API adapter from domain logic; Progressive Disclosure UX pattern to reduce cognitive load; cache stratification with different TTLs for market versus fundamental data; inverse threshold evaluation function to avoid duplicating signal logic across ratios.
Commit: feat: add financial ratios analysis engine with 12 metrics

Day 13 — Income Statement and Balance Sheet Viewer

Goal: Integrate real SEC filing data (10-K) via Finnhub to display Income Statement and Balance Sheet line items across the last 4 annual periods, adding a third analytical tab to the stock detail page.
What was done: Backend: Extended finnhub.service.ts with getFinancialStatements() calling /stock/financials-reported. Created financials.engine.ts as a normalization layer that translates Finnhub's XBRL array format (report.ic / report.bs with us-gaap_* concept keys) into typed NormalizedStatement objects. Added GET /api/v1/market/financials/:symbol endpoint. Frontend: Built StatementTable.tsx with professional financial formatting (B/M/K suffixes, accounting-style parentheses for negatives, sticky left column for mobile scrolling, zebra striping). Built FinancialStatements.tsx as a container with Income Statement / Balance Sheet sub-tabs. Integrated the third "Financial Statements" tab into the stock detail page alongside Overview and Financial Analysis.
Issues encountered & resolved: Finnhub's /stock/financials-reported returns financial data as arrays of objects under report.ic and report.bs, where each item has concept, label, value, unit — not the nested object structure (financials.incomeStatement) we initially assumed. The concept keys use XBRL underscore notation. Fixed by implementing a buildLookup() helper that converts the array into a Record for O(1) access, then mapping multiple possible XBRL keys per metric. Non-US tickers (e.g., AAPL.TO, AAPL.NE, AAPL.MX) return empty data or 403 errors because Finnhub's free tier covers primarily US exchanges. This is an API geography limitation. Decision: support US equities now; international exchanges will require a multi-provider adapter or paid-tier upgrade. Some metrics return null for companies with insufficient TTM data or non-US filings; the engine defaults to null and the frontend renders N/A gracefully.
Concepts learned: XBRL (eXtensible Business Reporting Language): the SEC standard for electronic financial reporting where every line item has a standardized machine-readable concept tag. CDR vs ADR: AAPL.TO is a Canadian Depositary Receipt, not the native US stock, explaining the lack of quote and fundamental data on US-centric APIs. API provider geography limitations: free-tier financial data APIs are overwhelmingly US-centric; building a truly global platform requires multi-provider aggregation or premium subscriptions. Array-to-Lookup normalization pattern: converting an API array response into a hash map for efficient key-based retrieval. Financial formatting conventions: parentheses for negative numbers, B/M/K scale suffixes, and tabular-nums CSS for perfect numeric alignment in tables.
Commit: feat: add income statement and balance sheet viewer with XBRL data normalization and multi-period tables

Day 14 — Cash Flow Statement & Annual/Quarterly Period Toggle

Goal: Add the Cash Flow Statement (Operating, Investing, Financing activities) and a toggle to switch between Annual (10-K) and Quarterly (10-Q) reporting periods.
What was done: Backend: Extended `finnhub.service.ts` with `freq` parameter (`annual` | `quarterly`) passed to Finnhub's `/stock/financials-reported` endpoint, using separate cache keys (`financials:SYM:annual` vs `financials:SYM:quarterly`) to prevent cache collisions; extended `financials.engine.ts` with `CASH_FLOW_KEYS` XBRL mappings and a 3-level fuzzy matching engine (exact XBRL concept key match, human-readable label pattern fallback, derived calculation fallback) to handle company-specific tag variations; added derived Free Cash Flow calculation (`OCF − CapEx`) when Finnhub does not expose the tag directly; added an unmapped key discovery system to log unknown XBRL concepts for future mapping expansion. Frontend: Rewrote `FinancialStatements.tsx` from a placeholder into an interactive component with three sub-tabs (Income Statement, Balance Sheet, Cash Flow) and an Annual/Quarterly period toggle; reused the existing `StatementTable` component without modifications for all three statement types.
Issues encountered & resolved: All financial data returned as N/A — root cause: Finnhub's response format changes when the `freq` query parameter is omitted vs included; the initial type declaration assumed a nested `financials: { incomeStatement: {...} }` structure, but the actual response uses `report: { ic: [...], bs: [...], cf: [...] }` arrays with `{ concept, label, value, unit }` items. Fixed by correcting the TypeScript type definition and moving the period filtering from client-side code to the `freq` API parameter. Node process zombie on Windows — after saving files, `nodemon` appeared to restart but the old Node process remained active on port 4000, serving stale code and suppressing new debug logs. Fixed by running `taskkill /F /IM node.exe` to force-kill all Node processes before restarting. Apple-specific XBRL tag variations — Apple uses custom `aapl_` prefixed concept keys and human labels like "Net sales", "Gross margin", and "Cost of sales" instead of standard "Revenue" and "Gross profit", causing exact key matching to fail. Fixed by implementing the label-pattern fallback layer in the fuzzy matching engine. Quarterly data empty — Finnhub's free tier returns only annual records when `freq` is omitted from the URL, so client-side filtering by `quarter !== 0` produced empty arrays. Fixed by explicitly passing `freq=quarterly` to the API and removing the manual quarter filter.
Concepts learned: Cash Flow Statement structure: Operating, Investing, and Financing activities; Free Cash Flow as the residual cash after capital expenditures (`OCF − CapEx`). XBRL concept key fragmentation: different companies file with different `us-gaap_*` tags and even custom prefixes (`aapl_*`), requiring fuzzy matching rather than hard-coded exact keys. Cache key composition: embedding the period into the cache key prevents stale data when toggling between annual and quarterly views. Windows process management: `nodemon` does not always terminate the previous Node process cleanly, requiring manual cleanup. Derived metric calculation: when a third-party API does not expose a metric directly (e.g., Free Cash Flow), computing it from available components is a valid and common engineering pattern in financial platforms.
Commit: feat: add cash flow statement and annual/quarterly period toggle with fuzzy XBRL matching engine.
Day 15 — Portfolio Tracker
Goal: Build a virtual portfolio system where authenticated users can create portfolios, buy/sell stocks with simulated positions, track unrealized P&L in real time via Finnhub live prices, and view asset allocation on the dashboard.
What was done: Database: Extended prisma/schema.prisma with Portfolio and PortfolioItem models featuring a one-to-many relation (Portfolio → PortfolioItem) and onDelete: Cascade to automatically clean up holdings when a portfolio or user is deleted. Backend: Created portfolio.routes.ts with six protected endpoints: GET /api/v1/portfolio (list all portfolios with live-enriched totals), POST /api/v1/portfolio (create empty portfolio), GET /api/v1/portfolio/:id (detail with per-holding P&L), POST /api/v1/portfolio/:id/buy (execute buy order with weighted average cost basis recalculation), POST /api/v1/portfolio/:id/sell (execute sell order with share-count validation and position closure), DELETE /api/v1/portfolio/:id (delete portfolio). Mounted portfolio routes in src/index.ts. Frontend: Built dashboard/portfolio/page.tsx as a portfolio list with creation form and delete action; built dashboard/portfolio/[id]/page.tsx as a detail view with a professional holdings table (symbol, shares, avg cost, live price, market value, P&L, P&L %) and an inline sell modal; updated dashboard/stock/[symbol]/page.tsx with a "Buy Shares" modal that lets users select a portfolio and purchase directly from the stock detail page; updated dashboard/page.tsx to aggregate total portfolio value and total return across all user portfolios.
Issues encountered & resolved:
Invalid or expired token on every protected request — root cause: jwt.ts was reading process.env.JWT_SECRET before dotenv loaded the .env file, causing tokens to be signed with undefined. Fixed by adding import 'dotenv/config' at the top of jwt.ts and reading process.env directly instead of importing config.ts.
ERR_MODULE_NOT_FOUND when importing ../config.js — root cause: jwt.ts resides in utils/ but attempted to import ./config.js (same directory) instead of ../config.js. Fixed by correcting the relative path, then ultimately resolved by removing the config.ts dependency entirely in favor of direct process.env access with dotenv.
Type error on jwt.sign overload — root cause: importing config.ts introduced TypeScript inference conflicts with the expiresIn option type. Fixed by reverting to process.env strings cast with as any.
Portfolio creation returned success but subsequent list reload showed Load failed — root cause: the PortfolioItem table did not exist in Supabase because prisma db push had been interrupted while using the connection pooler (port 6543), which does not support DDL. Fixed by manually creating the PortfolioItem table via Supabase SQL Editor using the direct connection (port 5432), then running npx prisma generate.
curl command failures in PowerShell — root cause: Windows PowerShell aliases curl to Invoke-WebRequest, which uses different parameter syntax. Fixed by using Invoke-WebRequest natively with -UseBasicParsing, ConvertTo-Json, and hashtables for headers.
Buy order succeeded but sell button showed no response and a runtime popup error — root cause: Next.js 15.5.20 does not support window.prompt() in the App Router runtime environment. Fixed by replacing the prompt() call with a fully controlled inline React modal (useState for showSellModal, sellSymbol, sellQty) inside dashboard/portfolio/[id]/page.tsx.
Sell API returned 500 after fixing the modal — root cause: the manually created PortfolioItem table in Supabase was missing the companyName column, which Prisma expected during findFirst({ include: { items: true } }) on the sell route. The error surfaced as PrismaClientKnownRequestError: P2022 — The column PortfolioItem.companyName does not exist in the current database. Fixed by dropping and recreating the PortfolioItem table via Supabase SQL Editor with all required columns (id, symbol, companyName, shares, avgCost, createdAt, updatedAt, portfolioId), then regenerating Prisma Client.
npx prisma generate failed with EPERM: operation not permitted — root cause: the backend Node process was still running and locking the Prisma query engine DLL (query_engine-windows.dll.node). Fixed by running taskkill /F /IM node.exe to force-kill all Node processes, deleting the node_modules\.prisma\client folder, and rerunning npx prisma generate.
Concepts learned: Weighted Average Cost Basis: recalculating the average purchase price when adding shares to an existing position using ((oldShares × oldAvgCost) + (newShares × newPrice)) / totalShares. Unrealized P&L: the paper profit/loss of an open position computed as (currentPrice - avgCost) × shares. Realized vs unrealized P&L distinction: unrealized is theoretical until the position is closed; realized requires a transaction history (planned for Day 16). Database consistency and validation: enforcing sellShares ≤ ownedShares on the backend before mutating state to prevent negative balances. Cascade deletes in relational design: Prisma's onDelete: Cascade automatically removes dependent PortfolioItem records when a parent Portfolio is deleted, maintaining referential integrity. Decimal arithmetic for financial data: using Prisma Decimal instead of Float to avoid floating-point rounding errors on prices and P&L calculations. JWT secret consistency: tokens are signed and verified with the same secret; any mismatch (e.g., from .env changes or dotenv loading order) invalidates all existing tokens. Systematic debugging: isolating frontend vs backend issues by testing the API directly with curl/Invoke-WebRequest to determine whether the server or the client is at fault. Next.js App Router runtime restrictions: window.prompt() and other browser APIs may be blocked or unsupported in certain Next.js runtime contexts; building custom controlled modals is the robust alternative. Database schema drift: manually creating tables via SQL can lead to column mismatches with the Prisma schema; the source of truth must remain schema.prisma, and manual SQL edits must exactly mirror it.
Commit: feat: add virtual portfolio tracker with buy/sell, live P&L and weighted average cost basis

Day 16 — Transaction History & Realized P&L

Goal: Add a Transaction table to log every buy/sell operation, compute Realized P&L using the FIFO (First In, First Out) method, and display a complete trade history on the portfolio detail page.
What was done: Extended prisma/schema.prisma with a new Transaction model containing fields: id, type (BUY/SELL), symbol, companyName, shares, price, totalAmount, realizedPnl, remainingShares, portfolioId, and createdAt. Added composite indexes on portfolioId and [portfolioId, symbol]. Created the Transaction table manually via Supabase SQL Editor after prisma db push failed silently on the connection pooler (port 6543). Regenerated Prisma Client with npx prisma generate after clearing the locked node_modules\.prisma\client folder. Added GET /api/v1/portfolio/:id/transactions endpoint to fetch the complete audit trail for a portfolio. Rewrote POST /api/v1/portfolio/:id/buy to run inside prisma.$transaction: it updates or creates the PortfolioItem (weighted average cost basis) and simultaneously creates a Transaction record with remainingShares equal to the purchased quantity. Rewrote POST /api/v1/portfolio/:id/sell to run inside prisma.$transaction: it fetches all open BUY lots (remainingShares > 0) ordered by createdAt ascending, consumes them lot-by-lot until the sell quantity is covered, calculates totalCostBasis using the exact historical purchase price of each lot, computes realizedPnl = (sellPrice × shares) − totalCostBasis, updates or deletes the PortfolioItem, and creates a SELL Transaction record with the realized profit/loss. Added a Transaction History section to dashboard/portfolio/[id]/page.tsx displaying a professional table with date, type badge (BUY/SELL), symbol, shares, price, total amount, and color-coded Realized P&L. Updated frontend/src/lib/api.ts with handleAuthError logic that detects 401/403 responses, clears the invalid token from localStorage, and redirects the user to /login with an alert. Updated the stock detail page (dashboard/stock/[symbol]/page.tsx) to always enable the "Buy Shares" button and show a contextual alert if no portfolio exists yet.
Issues encountered & resolved:
Property 'transaction' does not exist on type 'Omit<PrismaClient<...>>' — TypeScript error in portfolio.routes.ts after adding the Transaction model. Root cause: Prisma Client had not been regenerated after schema changes, so the generated types did not include the new model. Fixed by running taskkill /F /IM node.exe, deleting node_modules\.prisma\client, running npx prisma generate, and restarting VS Code + TypeScript Server.
Failed to run sql query: ERROR: 42P01: relation "Transaction" does not exist — the Transaction table was missing in Supabase. Root cause: npx prisma generate only updates TypeScript types, it does not create database tables. Fixed by executing a CREATE TABLE "Transaction" script directly in the Supabase SQL Editor, then re-running npx prisma generate for safety.
Portfolios and buy operations stopped working after schema changes — the frontend showed no error and the backend logged the request but returned no data. Root cause: the JWT token stored in localStorage had been signed with an old or mismatched JWT_SECRET (from a previous .env loading order issue), causing every protected request to fail silently with 403. Fixed by adding explicit handleAuthError logic in api.ts that intercepts 401/403 responses, clears the stale token, and forces a re-login. After logging out and back in, the new valid token allowed all operations to succeed.
PortfolioItem missing companyName column during sell — this was a residual schema drift from Day 15 where the table had been created manually without the column. Fixed by dropping and recreating the PortfolioItem table via Supabase SQL Editor to match schema.prisma exactly, then regenerating Prisma Client.
Concepts learned: FIFO (First In, First Out): the standard accounting method (IRS USA) where the oldest purchased shares are considered sold first, ensuring accurate cost basis tracking for tax reporting. Realized P&L vs Unrealized P&L: realized profit is concrete and locked in at the moment of sale; unrealized profit is theoretical and fluctuates with market price. Lot tracking: each BUY creates an immutable record (lot) with its own purchase price; the remainingShares field tracks how many shares from that specific lot are still unsold. Database transactions (ACID): prisma.$transaction guarantees atomicity — if any query inside the transaction fails, the entire operation is rolled back, preventing partial or inconsistent financial states. Audit trail immutability: financial transactions are append-only records that must never be deleted or modified, forming a complete legal history of all operations. Graceful degradation in FIFO: if no tracked lots exist (e.g., shares purchased before Day 16), the system falls back to the portfolio item's avgCost rather than crashing. Schema drift prevention: manual SQL table creation must exactly mirror schema.prisma; the Prisma schema must remain the single source of truth.
Commit: feat: add transaction history with FIFO realized P&L tracking

Day 17 — Price Alert System

Goal: Build a notification system where authenticated users can set price thresholds on stocks (above or below a target price) or percent-change triggers, poll live Finnhub quotes against active alerts in real time on the backend, and display triggered alerts as visual badges in the dashboard header with full read-status, simulation, reactivation, and delete capabilities.
What was done: Extended prisma/schema.prisma with a new PriceAlert model containing fields: id, userId, symbol, companyName, alertType (PRICE_TARGET or PERCENT_CHANGE), condition (ABOVE, BELOW, PERCENT_UP, PERCENT_DOWN), targetPrice, referencePrice, percentChange, isActive, isTriggered, triggeredAt, notifiedAt, and createdAt; added composite indexes on userId and [isActive, isTriggered]; created the table manually via Supabase SQL Editor after prisma db push failed on the connection pooler. Created backend/src/services/alert.service.ts as a dedicated polling service running a setInterval every 60000ms that fetches all active non-triggered alerts, deduplicates symbols, calls getQuote (reusing the existing 60-second TTL cache), evaluates PRICE_TARGET alerts by comparing currentPrice against targetPrice with ABOVE/BELOW logic, evaluates PERCENT_CHANGE alerts by computing ((currentPrice - referencePrice) / referencePrice) * 100 against percentChange with PERCENT_UP/PERCENT_DOWN logic, updates triggered alerts with isTriggered: true and triggeredAt: new Date(), and logs each trigger to console; implemented per-symbol try/catch so one API failure does not crash the entire polling cycle. Created backend/src/routes/alert.routes.ts with protected endpoints: GET /api/v1/alerts lists all user alerts ordered by createdAt desc; POST /api/v1/alerts creates an alert with Zod validation including a .refine() that enforces correct field combinations per alertType; POST /api/v1/alerts/:id/simulate forces an immediate trigger for testing/demo purposes; PUT /api/v1/alerts/:id/read marks a single triggered alert as read; PUT /api/v1/alerts/read-all marks all triggered alerts as read; PUT /api/v1/alerts/:id/reactivate resets an alert to active state clearing triggeredAt and notifiedAt; DELETE /api/v1/alerts/:id removes an alert using deleteMany({ id, userId }) for security. Mounted alert routes in backend/src/index.ts and called startAlertPolling() after app.listen so the poller begins on server startup. Created frontend/src/components/alerts/AlertForm.tsx with a form supporting both Price Target and Percent Change modes, condition dropdowns that adapt to the selected alert type, targetPrice or percentChange inputs, and automatic referencePrice injection for percent alerts. Created frontend/src/components/alerts/AlertBadge.tsx displaying a red notification badge with the unread triggered count in the dashboard header; clicking the badge calls PUT /api/v1/alerts/read-all and clears the badge; refreshes every 30 seconds. Created frontend/src/components/alerts/AlertList.tsx rendering all user alerts with conditional badges: Active (emerald), Triggered · NEW (red), Triggered (orange); shows alert details including formatted targetPrice or percentChange with referencePrice; displays triggeredAt timestamp; provides Test, Reactivate, and Delete action buttons per row. Updated frontend/src/app/dashboard/stock/[symbol]/page.tsx to import AlertForm and AlertBadge, add a "Set Price Alert" button opening a modal with the AlertForm, and include AlertBadge in the header navigation. Updated frontend/src/app/dashboard/page.tsx to import and render AlertList below the watchlist section and include AlertBadge in the header.
Issues encountered & resolved: EPERM: operation not permitted during npx prisma generate — root cause: the backend Node process was running and locking the Prisma query engine DLL (query_engine-windows.dll.node). Fixed by running taskkill /F /IM node.exe, deleting the node_modules.prisma\client folder, and rerunning npx prisma generate. The table public.PriceAlert does not exist (P2021) — root cause: npx prisma generate only generates TypeScript client types but does not create database tables. Fixed by executing a CREATE TABLE "PriceAlert" script directly in the Supabase SQL Editor, then rerunning npx prisma generate. Foreign key constraint column "UserId" does not exist — root cause: the SQL script used "UserId" with uppercase U but the actual column in the User table is "userId" with lowercase u; PostgreSQL is case-sensitive when identifiers are double-quoted. Fixed by rewriting the ALTER TABLE statement with the correct lowercase column name. Foreign key constraint cannot be implemented due to incompatible types uuid and text — root cause: the initial CREATE TABLE defined id and userId as UUID but the existing User.id column in the database is TEXT (because Prisma maps String @id to PostgreSQL TEXT, not UUID, even when the default is uuid()). Fixed by changing both id and userId columns to TEXT with DEFAULT (gen_random_uuid())::text to match the existing schema. Null constraint violation on targetPrice (P2011) when creating a PERCENT_CHANGE alert — root cause: the original Day-17 table creation had defined targetPrice as NOT NULL, but percent-change alerts do not use targetPrice; later schema.prisma was updated to make targetPrice optional (Decimal?) but the physical database column was never altered. Fixed by running ALTER TABLE "PriceAlert" ALTER COLUMN "targetPrice" DROP NOT NULL in the Supabase SQL Editor. Alert status badge confusion — triggered alerts displayed only "NEW" in red, which made users think the trigger had not fired; fixed by updating AlertList.tsx to display "Triggered · NEW" for unread triggered alerts and "Triggered" for read ones, making the state machine explicit.
Concepts learned: Event-driven architecture: moving from reactive request-response to proactive server-side polling where the backend continuously evaluates external state changes without user interaction. Backend polling vs frontend polling: server-side polling is more efficient for external API rate limits (one call per symbol regardless of user count), works 24/7 even with the browser closed, and provides a natural extension point for future push/email notifications. Idempotency: the poller skips alerts already marked isTriggered, ensuring the same condition does not generate infinite notifications. State machine design: an alert lifecycle moves from Active → Triggered → Read, with explicit flags (isTriggered, notifiedAt) preventing duplicate processing and providing clear UX. Deduplication: grouping alerts by symbol before calling Finnhub minimizes external API usage. Cache stratification: reusing the existing getQuote cache with 60-second TTL means the poller does not bombard Finnhub even when checking many alerts on the same symbol. Case sensitivity in PostgreSQL: double-quoted identifiers are case-sensitive, so "UserId" and "userId" are different columns; unquoted identifiers are folded to lowercase automatically. Schema drift: when Prisma schema and physical database diverge (e.g., NOT NULL constraints, missing columns), runtime errors occur; the Prisma schema must remain the single source of truth and manual SQL migrations must mirror it exactly. Type mapping between Prisma and PostgreSQL: Prisma String @id maps to TEXT in PostgreSQL, not UUID, even when using @default(uuid()). Graceful degradation in polling: wrapping each symbol evaluation in try/catch ensures one failed API call or malformed quote does not crash the entire polling cycle.
Commit: feat: add price alert system with backend polling, read status, simulation, reactivation and percent change

Day 18 — News Feed & Sentiment Analysis

Goal: Integrate a financial news API to display relevant news articles on each stock detail page, add keyword-based sentiment scoring (positive/negative/neutral), and cache news with a short TTL to balance freshness and API limits.
What was done: Extended finnhub.service.ts with getCompanyNews() calling /company-news endpoint, fetching the last 30 days of articles with a 300-second TTL cache key news:SYM. Created sentiment.engine.ts as a lexicon-based NLP engine with two dictionaries (POSITIVE_WORDS, NEGATIVE_WORDS) and analyzeSentiment() that tokenizes text, counts polarized terms, normalizes the score against total word count, and returns a SentimentResult with sentiment enum (POSITIVE, NEGATIVE, NEUTRAL) and a numeric score. Created aggregateSentiment() in sentiment.engine.ts to compute summary statistics (overall sentiment, positiveCount, negativeCount, neutralCount, averageScore) from an array of enriched news items. Added GET /api/v1/market/news/:symbol endpoint in market.routes.ts that fetches raw news, enriches each article with sentiment, computes the aggregate summary, and returns { news: [...], summary: {...} }. Created frontend/src/components/news/NewsCard.tsx as a clickable card rendering article image, source, datetime, headline, summary, and a color-coded sentiment badge (emerald for POSITIVE, red for NEGATIVE, slate for NEUTRAL). Created frontend/src/components/news/NewsFeed.tsx as a data container that fetches the news endpoint, manages loading/error/empty states, renders a summary counter grid (Positive / Negative / Neutral counts), and maps articles to NewsCard. Updated frontend/src/app/dashboard/stock/[symbol]/page.tsx to add a fourth tab "News" alongside Overview, Financial Analysis, and Financial Statements; integrated NewsFeed component under the news tab. Aligned frontend TypeScript interfaces with the backend response shape: NewsResponse contains news: NewsArticle[] and summary: SentimentSummary, ensuring type-safe access to res.data.news and res.data.summary.
Issues encountered & resolved: API contract mismatch between backend and frontend — the backend returned { data: { news, summary } } while the initial frontend code expected { data: Article[] }, causing the NewsFeed to fail silently with "Unknown error". Fixed by updating the frontend interface to match the nested structure and accessing res.data.news for the article list.
Concepts learned: Lexicon-based Sentiment Analysis: classification of text polarity using predefined dictionaries of positive and negative terms. Bag of Words (BoW): text representation that ignores grammar and word order, treating text as an unordered collection of tokens for frequency-based scoring. Tokenization and normalization: splitting text on non-word boundaries, converting to lowercase, and filtering short tokens to reduce noise before dictionary matching. API contract consistency: frontend and backend must share the exact same data structure; evolving a response shape requires synchronized updates on both sides. Graceful degradation: when Finnhub returns an empty news array for a symbol, the UI renders a friendly empty state instead of crashing.
Commit: feat: add news feed with keyword-based sentiment analysis

Day 19 — Sentiment Dashboard & Trend

Goal: Add a time-series sentiment chart showing sentiment evolution over the last 30 days, integrate an overall Sentiment Score metric into the stock Overview tab, add a client-side sentiment filter dropdown to the News tab, and extend the sentiment engine with a 3-day Simple Moving Average, cross-stock comparison, automated sentiment-based alerts, and CSV export.
What was done: Extended backend/src/services/sentiment.engine.ts with analyzeSentimentTrend() which aggregates news articles into daily buckets, computes the average sentiment score per day, determines the dominant sentiment via mode (frequency count), and calculates a 3-day Simple Moving Average (sma3) for each day starting from the third data point. Added GET /api/v1/market/sentiment-trend/:symbol endpoint in backend/src/routes/market.routes.ts to expose the trend data. Updated frontend/src/app/dashboard/stock/[symbol]/page.tsx to fetch and display an "Overall Media Sentiment" card in the Overview tab showing the aggregate sentiment badge, average score, and article count. Added a fifth "Sentiment" tab to the stock detail page housing the new trend chart and comparison controls. Created frontend/src/components/charts/SentimentTrendChart.tsx as a responsive Recharts LineChart rendering the daily averageScore as a solid emerald line with color-coded dots based on dominant sentiment, an orange dashed SMA3 line, a ReferenceLine at y=0, a fixed Y-axis domain of [-0.5, 0.5], and a custom tooltip displaying score, dominant sentiment, and article count. Extended SentimentTrendChart to support an optional compareSymbol prop: when populated, the component fires two parallel API requests via Promise.all, merges both datasets by date alignment, and renders four lines (Score, SMA3, Comp Score, Comp SMA3) with a legend and distinct colors (blue/violet for the comparison stock). Added a comparison input field and Compare/Clear buttons to the Sentiment tab UI. Updated frontend/src/components/news/NewsFeed.tsx with a client-side <select> filter allowing users to show All, Positive, Negative, or Neutral articles only, with a live counter showing "Showing X of Y articles". Extended backend/src/services/alert.service.ts to support a new SENTIMENT_TREND alert type: the poller fetches news for the symbol, runs analyzeSentimentTrend(), and triggers the alert when the latest available sma3 drops below the threshold stored in percentChange (treated as a decimal, e.g. 15 means -0.15). Updated backend/src/routes/alert.routes.ts Zod schema to accept SENTIMENT_TREND as a valid alertType with BELOW condition and percentChange required. Added a Download CSV button to SentimentTrendChart that generates a client-side Blob with headers Date,AverageScore,ArticleCount,DominantSentiment,SMA3, formats the main stock's trend array into CSV rows, and triggers a programmatic download via URL.createObjectURL.
Issues encountered & resolved: sma3 field returned empty in the API response during initial testing. Root cause: the backend Node process was running stale code from a previous session and had not picked up the updated sentiment.engine.ts. Fixed by force-killing all Node processes with taskkill /F /IM node.exe and restarting the dev server. The SentimentTrendChart initially failed to render comparison data when the second stock had fewer days than the primary. Fixed by implementing a date-alignment merge strategy using a Set of all dates and find() lookups, ensuring null gaps are handled gracefully by Recharts connectNulls.
Concepts learned: Time-bucketing: aggregating discrete events (news articles) into temporal buckets (days) to build a time-series. Simple Moving Average (SMA): a smoothing technique that reduces daily noise by averaging the last N periods, making underlying directional trends visible. Modalità (mode): the dominant sentiment of a day is the most frequent categorical value, which is more robust than the mean when polarity counts matter. Fixed Y-axis domain ([-0.5, 0.5]): locking the chart scale enables visual comparability across different stocks and time windows. Reference line at zero: provides an immediate visual threshold separating bullish from bearish narrative territory. Client-side filtering: filtering already-loaded data in React state eliminates redundant API calls and improves UI responsiveness. Parallel data fetching with Promise.all: loading two independent trend series simultaneously rather than sequentially. Event-driven architecture extension: adding a derived trigger type (SENTIMENT_TREND) to an existing polling service by chaining data transformation (news → trend → SMA → threshold check) without altering the core polling loop. Programmatic CSV generation in the browser: using Blob, URL.createObjectURL, and a transient <a> element to export structured data without a server round-trip.
Commit: feat: add sentiment trend chart, overall sentiment score, news filter, SMA3, comparison, alerts, and CSV export

Day 20 — US-Only Search Filter & Direct Symbol Fallback

Goal: Fix the search bug where non-US tickers (AAPL.TO, AAPL.MX) appeared in the autocomplete but returned Finnhub 403 errors, keep only US-listed symbols visible in the search bar, and handle the edge case of US share-class symbols (BRK.A, BRK.B) that Finnhub's search endpoint fails to match.
What was done: Backend: Created isUsListing filter predicate in backend/src/routes/market.routes.ts applied to the /api/v1/market/search endpoint: keeps symbols without an exchange suffix (AAPL) and drops non-US listings (AAPL.TO, SAP.DE, AAPL.MX) which Finnhub's free tier does not cover. Added isShareClass predicate using the anchored regex /.[AB]$/ to re-accept US share-class symbols (BRK.A, BRK.B), composed with isUsListing via logical OR. Regenerated the response count from the filtered array to keep metadata honest. Added directSymbolLookup() fallback executed only when the filtered search returns zero results: tries the query as a raw symbol in three variants (Q, Q.A, Q.B), validates each variant via getQuote() treating a non-zero price (c > 0) as the validity oracle, enriches valid hits with the company name from getCompanyProfile() (falling back to the symbol itself on profile failure), and synthesizes SearchResultItem objects compatible with the existing frontend dropdown. Added a separate business-level directLookupCache (300s TTL, key direct:q) so repeated empty searches do not re-hit Finnhub. Frontend: no changes needed — the SearchBar receives already-filtered, already-validated results. Verified Finnhub's canonical symbol format for Berkshire is BRK.A (dot), while the quote endpoint accepts both BRK.A and BRK-B.
Issues encountered & resolved: After the first filter implementation, searching "BRK" returned IBRK (Interactive Brokers) but not Berkshire Hathaway. Systematic isolation: (1) called Finnhub search API directly in the browser for q=BRK → confirmed raw response contained IBRK only; (2) tested q=BERKSHIRE → BRK.A appears, proving the symbol exists in Finnhub; (3) tested quote endpoint with both BRK-B and BRK.B → both return valid prices. Conclusion: not a filtering bug — Finnhub's search does substring matching on dot-less symbols (IBRK) but fails to match symbols containing a dot (BRK.A). Fixed by adding the direct symbol fallback instead of trying to fix the provider's search quirk. During debugging, the live Finnhub API key was accidentally pasted in plain text in the chat; rotated the key immediately via the Finnhub dashboard, updated backend/.env, and reinforced the rule: secrets live only in .env/cloud secrets, never in chat, screenshots, or commits.
Concepts learned: Two overlapping root causes can hide behind one symptom: the original symptom (AAPL.TO visible but 403) was our filtering gap, while the second symptom (BRK missing entirely) was a provider search quirk — separated by isolating layers (raw API → backend endpoint → frontend) and testing each directly. Documented heuristic: Finnhub encodes non-US listings as a dot suffix in the symbol field (AAPL.TO) while US share classes use single-letter suffixes (.A, .B) — exchange suffixes are always 2+ letters, which disambiguates the two cases. Predicate composition: small boolean functions combined with || are more testable and extensible than monolithic conditions. Validation by oracle: when an API returns 200 with c: 0 for invalid symbols instead of 404, a business-meaningful field (price > 0) becomes the validity oracle. Fallback cost discipline: the direct lookup fires only on empty results, keeping the 60 calls/minute rate limit safe; sequential for-loop over Promise.all to avoid bursts. Cache stratification: separate cache instances for raw API responses (service layer) vs business-rule results (route layer). Security incident handling: immediate key rotation after exposure, verification that no secret is committed to version control. Regression testing: re-running the original scenarios (Apple → only AAPL, SAP → only SAP) after each fix to ensure earlier fixes were not reopened.
Commit: fix: filter search to US listings with share-class support and direct symbol fallback

Day 21 — Company Comparison Page

Goal: Build a dedicated /dashboard/compare page for side-by-side comparison of two companies (quotes, key ratios from the analysis engine, price performance), reusing the Day-12 Analysis Engine and the existing StockChart component without modifications.
What was done: Backend: Added getCompanyData(symbol) helper in backend/src/routes/market.routes.ts that fetches quote, company profile, and full ratio analysis in parallel via Promise.all. Added GET /api/v1/market/compare/:symbolA/:symbolB endpoint running two getCompanyData calls inside an outer Promise.all (6 concurrent Finnhub calls, all covered by the existing cache stratification: 60s quote TTL, 3600s fundamentals TTL). Response shape: { a: { symbol, quote, profile, analysis }, b: {...} }. Frontend: Refactored SearchBar.tsx to accept optional props onSelect?: (symbol: string) => void and placeholder?: string (Inversion of Control via callback prop): when onSelect is provided the component delegates the selection to the parent; otherwise it falls back to the default router.push('/dashboard/stock/...') behavior, guaranteeing full backward compatibility with all existing call sites. Created src/components/compare/CompareTable.tsx: aligns the two ratio arrays by name into a unified row set (union of names, so missing metrics on one side render as N/A without breaking row sync), groups rows by the five existing categories (Valuation, Profitability, Leverage, Liquidity, Dividends), highlights the winning cell per row with an emerald background, and shows a "Ratio wins" scorecard in the footer with a disclaimer that context matters more than raw counts. Winner detection via pickWinner() using a LOWER_IS_BETTER set (P/E, PEG, EV/EBITDA, Debt-to-Equity) mirroring the getInverseSignal/getSignal duality from the analysis engine; ties and N/A rows excluded. Signal colors (emerald/red/muted) reused from the ratio card convention. Created src/app/dashboard/compare/page.tsx: two search slots each rendering the refactored SearchBar with onSelect wired to symbolA/symbolB state, a Swap button, loading spinner, error banner, and empty state; when both symbols are set, a useEffect fires the compare endpoint; renders CompareTable plus two side-by-side 60-day StockCharts for price performance. Added a "Compare" link to the header navigation in dashboard/stock/[symbol]/page.tsx.
Issues encountered & resolved: Header duplication identified as a code smell: the navigation block is copy-pasted across every dashboard page, so adding one menu item requires touching N files. Documented as a planned refactoring (extract a shared DashboardHeader component) but intentionally deferred to keep Day 21 scope focused. React key warning risk in CompareTable: category group headers inside a .map() rendered with a bare <> fragment would log "Each child in a list should have a unique key"; fixed by using <Fragment key={cat}> imported from React. TypeScript strictness on category access: (ratioA ?? ratioB)!.category is safe because allNames is the union of both ratio name arrays, so at least one side always exists for every row — documented with a comment.
Concepts learned: Inversion of Control via callback props: a component that navigates autonomously is less reusable than one that notifies what happened and lets the parent decide what to do — same principle behind <input onChange={...}>. Backward compatibility through optional props: extending a component's contract without breaking existing consumers. Cross-sectional analysis: comparing two companies at the same point in time, as opposed to time-series analysis (one company over time). Financial normalization: ratios are scale-independent by construction (per-share, percentages, or dimensionless ratios), making a $200B company and a $2B company directly comparable on fundamentals — while absolute values like market cap are shown for context only. Inter-company winner detection: requires knowing the directionality of each metric (higher-is-better vs lower-is-better), reusing the same threshold philosophy as the signal engine. Dataset alignment by key: union of names with find() lookups, the same merge strategy as the Day-19 date-alignment for sentiment comparison. Nested Promise.all for parallel resource fetching. Refactoring discipline: recognizing a code smell (duplicated header), documenting it, and deferring it deliberately rather than expanding scope mid-feature — scope control as an engineering skill.
Commit: feat: add company comparison page with side-by-side ratios, winner highlighting and dual search
Next Up
Day 22 — Discounted Cash Flow (DCF) Valuation Calculator: estimate a stock's intrinsic value by projecting Free Cash Flow from historical statements, discounting it with WACC, and adding a terminal value via the Gordon Growth Model; new dcf.engine.ts business layer on the backend, a GET /api/v1/market/dcf/:symbol endpoint, and a DCF tab on the stock detail page with editable assumptions (growth rate, WACC, terminal growth) and a sensitivity table. Concepts: time value of money, discounted cash flow, WACC (CAPM-derived cost of equity), terminal value, margin of safety, sensitivity analysis, separating assumptions from computation. 